name: Backend CI/CD

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - 'k8s/backend-deployment.yaml'
      - 'k8s/ingress.yaml'

env:
  ACR_LOGIN_SERVER: ${{ secrets.ACR_NAME }}.azurecr.io

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set IMAGE_TAG
        run: echo "IMAGE_TAG=$(echo $GITHUB_SHA | cut -c1-7)" >> $GITHUB_ENV

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Build & Push Backend
        run: |
          az acr login --name ${{ secrets.ACR_NAME }}
          docker build -t $ACR_LOGIN_SERVER/backend:${{ env.IMAGE_TAG }} ./backend
          docker push $ACR_LOGIN_SERVER/backend:${{ env.IMAGE_TAG }}

      - name: Get AKS Credentials
        run: |
          az aks get-credentials --resource-group ${{ secrets.AKS_RESOURCE_GROUP }} \
                                 --name ${{ secrets.AKS_CLUSTER_NAME }} \
                                 --overwrite-existing

      - name: Update Backend Deployment
        run: |
          kubectl set image deployment/backend-deployment backend=$ACR_LOGIN_SERVER/backend:${{ env.IMAGE_TAG }} --record
          kubectl rollout status deployment/backend-deployment --timeout=180s

      - name: Apply Ingress (if changed)
        run: kubectl apply -f k8s/ingress.yaml